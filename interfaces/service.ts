export interface IBetHistoryArgs {
    app: string;
    user_id: string;
    operator_id: string;
    limit: number;
}
export interface IBetDetailArgs {
    app: string;
    user_id: string;
    operator_id: string;
    lobby_id: string;
}